<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <title>Bold Brew</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Custom Tailwind Colors -->
    <style>
      :root {
        --color-brew-brown: #8C533C;
        --color-brew-light: #F8F5F2;
        --color-brew-dark: #1A1A1A;
      }
      .bg-brew-light { background-color: var(--color-brew-light); }
      .bg-brew-dark { background-color: var(--color-brew-dark); }
      .text-brew-brown { color: var(--color-brew-brown); }
      .text-brew-dark { color: var(--color-brew-dark); }
      .border-brew-brown { border-color: var(--color-brew-brown); }
      .font-bebas { font-family: 'Bebas Neue', sans-serif; }
    </style>

    <!-- The conflicting line has been removed -->
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="min-h-full bg-white text-brew-dark font-inter antialiased">
    <div class="min-h-screen flex flex-col">
      <%= render "shared/navbar" %>
      <main class="flex-1">
        <%= yield %>
      </main>
      <%= render "shared/footer" %>
    </div>
  </body>
</html>
