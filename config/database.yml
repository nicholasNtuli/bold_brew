default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: bold_brew_development

test:
  <<: *default
  database: bold_brew_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
  sslmode: require
